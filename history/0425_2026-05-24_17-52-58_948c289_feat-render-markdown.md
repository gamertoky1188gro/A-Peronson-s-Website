# 0425 — feat: render assistant messages as Markdown

**Commit:** `948c289bcb1099fb6276d73a9692c9da1ecdf018`
**Parent:** `89dddda545cc9b50e5d018112e00708fe12accf2`
**Author:** gamertoky1188gro
**Date:** 2026-05-24 17:52:58 +0600

## High-Level Summary
Imports the existing `MarkdownMessage` component and uses it to render all non-animating assistant messages in the FloatingAssistant chat. Streaming chunks and completed replies now render with full markdown support (bold, italic, code blocks, lists, tables, links, images).

## File-by-File Breakdown
| File | Change |
|------|--------|
| `src/components/FloatingAssistant.jsx` | 3 insertions |

## Detailed Diff Analysis
```diff
 import BotLogo from "./ui/BotLogo";
+import MarkdownMessage from "./chat/MarkdownMessage";
...
+                  ) : msg.role === "assistant" ? (
+                    <MarkdownMessage text={msg.text} />
                   ) : (
                     msg.text
                   )}
```

## Why This Change
Users were seeing raw markdown syntax (e.g. \*\*bold\*\*, ```code```) in AI responses. This makes them render properly.

## Was It Useful
Yes — directly improves UX for the AI assistant feature.

## Impact Analysis
**Low-medium.** Changes only the rendering path for assistant messages. The `MarkdownMessage` component was already tested/used elsewhere.

## Relationships
Prerequisite for 0426 (instructing AI to use Markdown), 0427 (escape backticks in system prompt).

## Confidence Notes
High — straightforward component reuse.
