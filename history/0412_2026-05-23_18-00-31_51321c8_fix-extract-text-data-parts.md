# Commit 0412 — `51321c8effb`

| Field       | Value                                                            |
| ----------- | ---------------------------------------------------------------- |
| Commit Hash | `51321c8effb1941a0f6021f4396b510bc0c583e6`                       |
| Parent Hash | `1c6a7af724cb3840febc534f9fe8ca2ec9f49ccb`                       |
| Author      | gamertoky1188gro                                                 |
| Date        | 2026-05-23 18:00:31 +0600                                        |
| Subject     | fix: extract text from response.data.parts instead of info.parts |

---

## High-Level Summary

Changes both the init test and `callOpencode` to extract response text from `response.data.parts` (array of typed parts) instead of the old `response.data.info.parts` path. Removes ~12 fallback extraction attempts that were trying different property names.

---

## Files Changed

| File                                  | Status   | Insertions | Deletions |
| ------------------------------------- | -------- | ---------- | --------- |
| `server/services/assistantService.js` | modified | 44         | 0         |

**1 file changed, 16 insertions, 28 deletions**

---

## Why

The opencode API response shape changed — the correct path is `response.data.parts[].text` where each part has a `type` field. The old code tried many fallbacks for different API shapes; this simplifies it to the correct one.

---

## Was It Useful

Yes — fixes text extraction from opencode API responses.

---

## Impact

Medium — core fix for AI response parsing.

---

## Relationships

Part of opencode integration.

---

## Confidence

High.
