# Commit 0393 — `d28a5735a2b`

| Field       | Value                                                                 |
| ----------- | --------------------------------------------------------------------- |
| Commit Hash | `d28a5735a2bb3a2fb08ff1713181b1c1958cd67b`                            |
| Parent Hash | `563d75514ec186c186484fe51f7f8543c60dfe56`                            |
| Author      | gamertoky1188gro                                                      |
| Date        | 2026-05-22 13:27:15 +0600                                             |
| Subject     | fix: lazy-eval AI paths to avoid crash when HARAM_DETECTION_DIR unset |

---

## High-Level Summary

Refactors `aiModerationService.js` to lazily evaluate all file paths. Previously, module-level `path.join` calls for `VENV_DIR`, `VENV_PY_WIN`, etc. would crash at import time if `HARAM_DETECTION_DIR` was null. Now all path construction is wrapped in functions that check `ensureDir()` first.

---

## Files Changed

| File                                     | Status   | Insertions | Deletions |
| ---------------------------------------- | -------- | ---------- | --------- |
| `server/services/aiModerationService.js` | modified | 35         | 0         |

**1 file changed, 26 insertions, 9 deletions**

---

## Why

Module-level `path.join(null, '.venv')` throws. Lazy evaluation prevents crash at import time.

---

## Was It Useful

Yes — companion fix to 0392, ensuring the module can be imported without error.

---

## Impact

Small.

---

## Relationships

Follow-up to 0392.

---

## Confidence

High.
