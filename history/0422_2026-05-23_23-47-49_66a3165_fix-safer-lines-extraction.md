# 0422 — fix: safer lines extraction in searchCodeContext

**Commit:** `66a3165283b2d51ee26a385d674842bbcd862f2e`
**Parent:** `95edbdbeccb3b5c155233e1462e6cd68c123b1a7`
**Author:** gamertoky1188gro
**Date:** 2026-05-23 23:47:49 +0600

## High-Level Summary
Refactors the `snippets` mapping in `searchCodeContext` into an explicit multi-statement block with named variables (`rawLines`, `linesStr`) for clarity and safety. No functional change — same inline ternary logic extracted for readability.

## File-by-File Breakdown
| File | Change |
|------|--------|
| `server/services/assistantService.js` | 9 insertions, 5 deletions |

## Detailed Diff Analysis
```diff
- const snippets = matches.slice(0, 8).map((m) => ({
-   file: m.path || "",
-   line: m.line_number || null,
-   snippet: (Array.isArray(m.lines) ? m.lines.join("\n") : (m.lines || "")).trim().substring(0, 320),
- }));
+ const snippets = matches.slice(0, 8).map((m) => {
+   const rawLines = m.lines;
+   const linesStr = Array.isArray(rawLines) ? rawLines.join("\n") : String(rawLines || "");
+   return {
+     file: m.path || "",
+     line: m.line_number || null,
+     snippet: linesStr.trim().substring(0, 320),
+   };
+ });
```

## Why This Change
Improves code readability and robustness by using explicit intermediate variables. Also uses `String()` coercion as a fallback instead of relying on `|| ""`.

## Was It Useful
Moderate — clarifies intent, but the logic is identical in behavior.

## Impact Analysis
**Trivial.** Pure refactor, no observable behavior change.

## Relationships
Direct follow-up to 0421 which introduced the array check.

## Confidence Notes
High — straightforward refactor.
